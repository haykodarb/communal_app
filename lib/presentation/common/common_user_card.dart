import 'package:flutter/material.dart';

class CommonUserCard extends StatelessWidget {
  const CommonUserCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
